/**
* Generated by unionj-generator.
* Don't edit!
*
* @module Upload
*/
import BizService from "./BizService";
import type {
  UploadResp,
} from "./types"

export class UploadService extends BizService{

  constructor(axios: any) {
    super(axios);
  }

  /**
  * POST /upload
  *
  * @param formData 
  * @returns Promise<UploadResp> 
  */
  postUpload(
    formData: FormData,
  ) :Promise<UploadResp> {
    let client = this.axios.post
    if(this.axios.$post) {
      client = this.axios.$post
    }
    return client(this.addPrefix(`/upload`),
          formData,
          {
          }
        )
  }

}

export default UploadService;

