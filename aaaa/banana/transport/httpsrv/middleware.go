/**
* Generated by go-doudou v2.3.3.
* You can edit it as your need.
 */
package httpsrv

import (
	"github.com/unionj-cloud/go-doudou/v2/toolkit/zlogger"
	"net/http"
)

func Custom(inner http.Handler) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		zlogger.Info().Msg(r.RequestURI + " from banana service Custom middleware 1")
		inner.ServeHTTP(w, r)
		zlogger.Info().Msg(r.RequestURI + " back from banana service Custom middleware 4")
	})
}

func Custom2(inner http.Handler) http.Handler {
	return http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		zlogger.Info().Msg(r.URL.String() + " from banana service Custom2 middleware 2")
		inner.ServeHTTP(w, r)
		zlogger.Info().Msg(r.URL.String() + " back from banana service Custom2 middleware 3")
	})
}
